package tests_test

import "github.com/sethcarney/mdm/internal/lock"

// lockName mirrors the CLI's project lock file name so these tests follow
// a rename of lock.ProjectLockName without edits.
const lockName = lock.ProjectLockName
