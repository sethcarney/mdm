---
name: emoji-test-skill
description: fixture used to verify emoji variation sequences do not block installs
---

# Emoji test skill

This fixture is test data. Its README uses ordinary picker emoji such as
the warning sign, which carry a U+FE0F variation selector.
