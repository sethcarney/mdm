# Hidden character fixture

This file intentionally contains hidden Unicode characters so mdm can test that
skill installs block suspicious markdown before copying files.

The next line contains a zero-width space between "zero" and "width":

zero​width

The next line contains a right-to-left override between "bidi" and "text":

bidi‮text

The next line contains a soft hyphen between "soft" and "hyphen":

soft­hyphen
